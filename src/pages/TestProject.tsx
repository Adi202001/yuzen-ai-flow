import { SimpleProjectForm } from "@/components/dashboard/SimpleProjectForm";

export default function TestProject() {
  return (
    <div className="min-h-screen bg-gradient-surface p-8">
      <SimpleProjectForm />
    </div>
  );
}
